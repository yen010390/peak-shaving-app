streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
lightgbm>=4.0
xgboost>=2.0
openpyxl>=3.1

# Optional: needed only for the LSTM / FCN / ResNet / KNN-DTW classifiers in Table 5.
# The app runs fine without them -- those four models are just skipped. They are
# heavy (tensorflow especially) and slow to install; comment them out if you don't
# need them.
tensorflow>=2.15
tslearn>=0.6
